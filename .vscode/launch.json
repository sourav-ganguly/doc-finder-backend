{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run API with uv",
            "type": "node-terminal",
            "request": "launch",
            "command": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
        }
    ]
}
